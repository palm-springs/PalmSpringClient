export interface MemberProps {
  email: string;
  id: number;
  job: string;
  nickname: string;
  role: string;
  thumbnail: string;
}

export interface emailData {
  emailValue: string;
  verification: boolean;
}
